# Cursor Project Setup

This directory keeps project-specific Cursor assets:

- `commands/` - reusable command notes and workflows.
- `rules/` - persistent AI rules in `.mdc` format.
- `context/` - project context, decisions, and operating notes.
- `tools/` - local tool notes and helper documentation.

